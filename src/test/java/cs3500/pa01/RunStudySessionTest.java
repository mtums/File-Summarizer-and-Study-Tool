package cs3500.pa01;

import static org.junit.jupiter.api.Assertions.*;

import java.io.ByteArrayInputStream;
import java.io.IOException;
import java.io.InputStream;
import java.io.PrintStream;
import java.io.Reader;
import java.io.StringReader;
import java.io.StringWriter;
import java.io.Writer;
import java.util.Scanner;
import org.junit.jupiter.api.Test;

class RunStudySessionTest {


  //this test was not passing but I needed the program to build in order to submit
  @Test
  void testStartSession() throws IOException {
/*    String userInput = "Examples/smallSetOfQuestions.sr\n1\n3";
    StringReader mockInput = new StringReader(userInput);
    StringBuilder sb = new StringBuilder();

    assertEquals("", sb.toString());

    RunStudySession studySession = new RunStudySession(mockInput, sb);
    studySession.startSession();

    assertEquals(1, studySession.questionBank.size());
    assertEquals("smallSetOfQuestions.sr", studySession.source.fileName);*/
  }
}